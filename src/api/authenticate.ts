

export type User = {
    username: string;
}

//export function authenticate(): Promise<User | undefined> {
  //  return new Promise((resolve,reject) => {
    //    setTimeout(() => resolve({id: "0", name: "Andrew"}),1000);
    //});
//}